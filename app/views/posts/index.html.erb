<div class="container">
  <h1 class="my-4 text-center">Latest Posts</h1>

  <div class="row">
    <% @posts.each do |post| %>
      <div class="col-md-4">
        <div class="card mb-4">
          <div class="card-body">
            <h5 class="card-title"><%= link_to post.title, post_path(post) %></h5>
            <p class="card-text"><%= truncate(post.content, length: 100) %></p>
            <p class="text-muted"><small>By <%= post.user.email %>, <%= time_ago_in_words(post.created_at) %> ago</small></p>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <%= paginate @posts, theme: 'bootstrap-4' %>
</div>
